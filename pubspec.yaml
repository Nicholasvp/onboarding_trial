name: onboarding_trial
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.1 <4.0.0'

dependencies:
  circular: ^0.0.5
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  google_fonts: ^4.0.0
  sleek_circular_slider: ^2.0.1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
